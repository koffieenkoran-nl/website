# koffieenkoran.nl

Gebouwde uitvoer van de site (React + Vite), geen broncode. De broncode staat bij Ahmet.

Bijwerken: vervang alles behalve `vercel.json`, `.gitignore` en dit bestand door de nieuwe
`dist/`, commit en push. `vercel.json` regelt op Vercel de doorverwijzing naar `index.html`
en de cache op `/assets/`.
